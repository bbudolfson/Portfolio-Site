#!/usr/bin/env bash
# Same as npm run dev:reset — works without global npm when .tools/node exists.
set -euo pipefail
ROOT="$(cd "$(dirname "$0")" && pwd)"
if [[ -x "$ROOT/.tools/node/bin/node" ]]; then
  export PATH="$ROOT/.tools/node/bin:$PATH"
fi
exec bash "$ROOT/scripts/dev-reset.sh"
